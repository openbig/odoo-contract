<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data noupdate="1">
    	<record id="template_portal_invitation" model="email.template">
            <field name="name">Portal Invitation</field>
            <field name="email_from">${object.user_from.email}</field>
            <field name="email_to">${object.user_to.email}</field>
            <field name="subject">Your Odoo account at ${object.company}!</field>
            <field name="email_to">${object.email|safe}</field>
            <field name="lang">${object.lang}</field>
            <field name="model_id" ref="portal.model_portal_wizard_user"/>
            <field name="auto_delete" eval="True"/>
            <field name="body_html"><![CDATA[
Dear ${object.name},

You have been given access to ${object.company}'s ${object.portal}.

Your login account data is:
  Username: ${object.login}
  Portal: ${object.portal_url}
  Database:${object.db}


You can set or change your password via the following url:
   ${object.signup_url}

${object.welcome_message}
--
Odoo - Open Source Business Applications
http://www.odoo.com
            ]]></field>
        </record>
    </data>
</openerp>